#include "main.h"
/**
 * _abs - absolute number
 * @num: the integer to be checked
 * Return: absolute value
 */
int _abs(int num)
{
	if (num >= 0)
	{
		return (num);
	}
	else
	{
		return (-num);
	}
}
