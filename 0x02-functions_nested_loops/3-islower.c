#include "main.h"
/**
 * _islower - printing lower case
 *@c: the char to be checked
 * Return: 1(lowercase), else 0
 *
 */
int _islower(int c)
{
	if (c >= 'a' && c <= 'z')
	{
		return (1);
	}
	else
	{
		return (0);
	}
	return (0);
}
